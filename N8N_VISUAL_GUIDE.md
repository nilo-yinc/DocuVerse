# 🎨 n8n Webhook Visual Setup Guide

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                   │
│           DocuVerse Studio + n8n Webhook Integration             │
│                    Visual Setup Guide                            │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 🚀 Step 1: Install Docker Desktop

```
┌──────────────────────────────┐
│   Download Docker Desktop    │
│                              │
│  https://docker.com          │
│                              │
│  ▼ Windows                   │
│  ▼ macOS                     │
│  ▼ Linux                     │
└──────────────────────────────┘
        │
        ▼
┌──────────────────────────────┐
│   Install & Restart PC       │
└──────────────────────────────┘
        │
        ▼
┌──────────────────────────────┐
│   Start Docker Desktop       │
│   (Wait for green icon)      │
└──────────────────────────────┘
```

## 🔧 Step 2: Run Setup Script

```
D:\Desktop\AutoSRS\
    │
    ├── Double-click: setup-n8n.bat
    │
    ▼
┌─────────────────────────────────────┐
│  ✓ Checking Docker...               │
│  ✓ Starting n8n...                  │
│  ✓ Creating containers...           │
│  ✓ n8n is running!                  │
│                                     │
│  Access: http://localhost:5678      │
└─────────────────────────────────────┘
```

## 🌐 Step 3: Setup n8n Account

```
Open Browser → http://localhost:5678
        │
        ▼
┌─────────────────────────────┐
│   Create n8n Account        │
│                             │
│   Email: [____________]     │
│   Password: [__________]    │
│                             │
│   [ Create Account ]        │
└─────────────────────────────┘
        │
        ▼
┌─────────────────────────────┐
│   n8n Dashboard             │
│                             │
│   ☰ Workflows               │
│   ⚙ Settings                │
│   📊 Executions             │
└─────────────────────────────┘
```

## 📥 Step 4: Import Workflows

```
n8n Dashboard
    │
    ├── Click "Workflows"
    │       │
    │       ├── Click "+ Add workflow"
    │       │       │
    │       │       └── Click "..." → "Import from File"
    │       │
    │       ▼
    ┌─────────────────────────────────┐
    │   Select File:                  │
    │                                 │
    │   📁 n8n_workflows/             │
    │     ├─ srs-generated-workflow   │
    │     ├─ project-created-workflow │
    │     └─ health-check-workflow    │
    └─────────────────────────────────┘
            │
            ▼
    ┌─────────────────────────────────┐
    │   Workflow Imported!            │
    │                                 │
    │   [🔘 Activate] ← Click this!   │
    └─────────────────────────────────┘
```

## ⚙️ Step 5: Configure Environment

```
Open: D:\Desktop\AutoSRS\.env
    │
    ▼
┌─────────────────────────────────────────┐
│  # n8n Webhook Configuration            │
│  N8N_ENABLED=false                      │
│  N8N_WEBHOOK_URL=http://localhost:5678  │
│  N8N_WEBHOOK_SECRET=your_secret         │
└─────────────────────────────────────────┘
    │
    │ Change to:
    ▼
┌─────────────────────────────────────────┐
│  N8N_ENABLED=true  ← Change to true     │
│  N8N_WEBHOOK_URL=http://localhost:5678  │
│  N8N_WEBHOOK_SECRET=random_key_123      │
└─────────────────────────────────────────┘
    │
    │ Save file!
    ▼
```

## 🔄 Step 6: Restart Backend

```
Terminal/PowerShell
    │
    ▼
┌──────────────────────────────┐
│  cd D:\Desktop\AutoSRS\      │
│  cd backend                  │
│  npm run dev                 │
└──────────────────────────────┘
    │
    ▼
┌──────────────────────────────┐
│  Server running on port 5000 │
│  [n8n] Webhook enabled       │
│  ✓ Connected to n8n          │
└──────────────────────────────┘
```

## ✅ Step 7: Test & Verify

```
1. Test Connection
┌─────────────────────────────────────────┐
│  Terminal/Browser:                      │
│                                         │
│  curl -X POST                           │
│    http://localhost:5000/api/webhooks/  │
│    test                                 │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│  { "success": true }  ✓                 │
└─────────────────────────────────────────┘


2. Generate SRS
┌─────────────────────────────────────────┐
│  DocuVerse Frontend                     │
│  → Create New Project                   │
│  → Fill Form                            │
│  → Generate SRS                         │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│  Backend Console:                       │
│  [n8n] SRS generation notification sent │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│  n8n Dashboard → Executions:            │
│  ✓ srs-generated workflow executed      │
│  ✓ Notifications sent                   │
└─────────────────────────────────────────┘
```

## 🎉 Success!

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│              ✅ Setup Complete!                         │
│                                                         │
│   Your DocuVerse Studio now has:                       │
│                                                         │
│   ✓ n8n running on http://localhost:5678               │
│   ✓ Webhooks enabled                                   │
│   ✓ Workflows imported & active                        │
│   ✓ Backend connected                                  │
│                                                         │
│   Next: Customize workflows & add integrations!        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Architecture Overview

```
┌──────────────┐
│   User       │
│   Actions    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│     DocuVerse Backend                │
│                                      │
│  ┌─────────────────────────────┐    │
│  │  routes/projects.js         │    │
│  │  ├─ Create Project          │    │
│  │  ├─ Generate SRS            │    │
│  │  └─ Generate Prototype      │    │
│  └──────────┬──────────────────┘    │
│             │                        │
│  ┌──────────▼──────────────────┐    │
│  │  n8n-webhook.service.js     │    │
│  │  ├─ notifyProjectCreated()  │    │
│  │  ├─ notifySRSGenerated()    │    │
│  │  └─ notifyPrototype...()    │    │
│  └──────────┬──────────────────┘    │
└─────────────┼────────────────────────┘
              │ HTTP POST
              ▼
┌─────────────────────────────────────┐
│         n8n Container               │
│                                     │
│  ┌────────────────────────────┐    │
│  │  Webhook Endpoints         │    │
│  │  /webhook/srs-generated    │    │
│  │  /webhook/project-created  │    │
│  │  /webhook/prototype-...    │    │
│  └──────────┬─────────────────┘    │
│             │                       │
│  ┌──────────▼─────────────────┐    │
│  │  Workflow Engine           │    │
│  │  ├─ Extract Data           │    │
│  │  ├─ Process Logic          │    │
│  │  └─ Route Actions          │    │
│  └──────────┬─────────────────┘    │
└─────────────┼───────────────────────┘
              │
       ┌──────┴──────┬─────────┐
       │             │         │
       ▼             ▼         ▼
┌──────────┐  ┌──────────┐  ┌──────────┐
│  Slack   │  │  Email   │  │  Others  │
│  Teams   │  │  Gmail   │  │  300+    │
└──────────┘  └──────────┘  └──────────┘
```

---

## 🎨 Workflow Example: SRS Generated

```
SRS Generated Event
        │
        ▼
┌─────────────────────┐
│  Webhook Received   │
│  Event: srs.gener.. │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│  Validate Event     │
│  Type Check         │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│  Extract Data       │
│  - Project Name     │
│  - Domain           │
│  - Links            │
└─────────┬───────────┘
          │
    ┌─────┴─────┬──────────────┐
    │           │              │
    ▼           ▼              ▼
┌───────┐  ┌────────┐    ┌─────────┐
│ Slack │  │ Email  │    │ Google  │
│ Post  │  │ Send   │    │ Sheets  │
└───┬───┘  └───┬────┘    └────┬────┘
    │          │              │
    └──────────┴──────────────┘
               │
               ▼
       ┌───────────────┐
       │  Respond OK   │
       │  Success: true│
       └───────────────┘
```

---

## 🔐 Security Setup

```
.env File Security
        │
        ├─► N8N_WEBHOOK_SECRET=?
        │
        ▼
Generate Strong Secret:
┌────────────────────────────────────┐
│  PowerShell/Terminal:              │
│                                    │
│  node -e "console.log(             │
│    require('crypto')               │
│      .randomBytes(32)              │
│      .toString('hex')              │
│  )"                                │
└────────────────────────────────────┘
        │
        ▼
┌────────────────────────────────────┐
│  Output:                           │
│  a3f5b2c8d9e1f4g7h8i9j0k1l2m3n4o5 │
└────────────────────────────────────┘
        │
        ▼
┌────────────────────────────────────┐
│  Copy to .env:                     │
│  N8N_WEBHOOK_SECRET=a3f5b2c8...   │
└────────────────────────────────────┘
```

---

## 📚 Quick Reference

```
┌─────────────────────────────────────────────────┐
│  Common Commands                                │
├─────────────────────────────────────────────────┤
│  Start n8n:                                     │
│    docker-compose up -d                         │
│                                                 │
│  Stop n8n:                                      │
│    docker-compose down                          │
│                                                 │
│  View logs:                                     │
│    docker-compose logs -f n8n                   │
│                                                 │
│  Restart n8n:                                   │
│    docker-compose restart                       │
│                                                 │
│  Check status:                                  │
│    docker ps                                    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  Important URLs                                 │
├─────────────────────────────────────────────────┤
│  n8n Dashboard:                                 │
│    http://localhost:5678                        │
│                                                 │
│  Backend API:                                   │
│    http://localhost:5000                        │
│                                                 │
│  Frontend:                                      │
│    http://localhost:5173                        │
│                                                 │
│  Webhook Status:                                │
│    http://localhost:5000/api/webhooks/status    │
└─────────────────────────────────────────────────┘
```

---

## 🐛 Troubleshooting Visual Guide

```
Problem: n8n not accessible
        │
        ├─► Check Docker running?
        │       │
        │       ├─► Yes → Check port 5678
        │       │             │
        │       │             └─► In use? Change port
        │       │
        │       └─► No → Start Docker Desktop
        │
Problem: Webhooks not firing
        │
        ├─► Check N8N_ENABLED=true in .env
        │       │
        │       └─► Restart backend
        │
        ├─► Check workflows active in n8n
        │       │
        │       └─► Click "Active" toggle
        │
        └─► Check backend logs for [n8n] messages
```

---

## ✨ Customization Ideas

```
Basic Setup (Done ✓)
        │
        ├─► Add Slack Integration
        │       │
        │       ├─► Get Slack webhook URL
        │       ├─► Add to n8n workflow
        │       └─► Test with SRS generation
        │
        ├─► Add Email Alerts
        │       │
        │       ├─► Configure SMTP in n8n
        │       ├─► Add recipient email
        │       └─► Test notification
        │
        ├─► Add Google Sheets Logging
        │       │
        │       ├─► Connect Google account
        │       ├─► Add Sheets node
        │       └─► Log all SRS generations
        │
        └─► Add Discord Webhook
                │
                ├─► Get Discord webhook URL
                ├─► Add HTTP Request node
                └─► Format message with project details
```

---

## 🎓 Documentation Map

```
Start Here:
┌─────────────────────────────────┐
│  N8N_README.md                  │
│  Quick 5-minute overview        │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│  N8N_SETUP_CHECKLIST.md         │
│  Step-by-step setup guide       │
└───────────────┬─────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│  N8N_QUICK_REFERENCE.md         │
│  Commands & examples            │
└───────────────┬─────────────────┘
                │
                ▼
        Need more details?
                │
    ┌───────────┴───────────┐
    │                       │
    ▼                       ▼
┌─────────────┐     ┌──────────────────┐
│ N8N_        │     │ N8N_             │
│ INTEGRATION │     │ ARCHITECTURE     │
│ .md         │     │ .md              │
│ Complete    │     │ Visual diagrams  │
│ guide       │     │ & flows          │
└─────────────┘     └──────────────────┘
```

---

## 🎯 Success Indicators

```
✓ n8n Dashboard accessible
    http://localhost:5678
        │
        ▼
✓ Workflows imported & active
    Green "Active" toggle in n8n
        │
        ▼
✓ Backend connected
    [n8n] logs in console
        │
        ▼
✓ Webhook test passes
    /api/webhooks/test returns success
        │
        ▼
✓ SRS generation triggers workflow
    Check n8n → Executions
        │
        ▼
    🎉 FULLY WORKING!
```

---

**For detailed instructions, see: `N8N_INTEGRATION.md`**
**For quick commands, see: `N8N_QUICK_REFERENCE.md`**
**For troubleshooting, see: `N8N_INTEGRATION.md` (Troubleshooting section)**

🚀 **Ready to automate!**
